# Web framework
fastapi
uvicorn[standard]

# Database (MongoDB)
pymongo[srv]>=4.17.0
dnspython>=2.8.0
# Optional: pymysql kept for running scripts/migrate_mysql_to_mongo.py
pymysql

# Settings / environment variables
pydantic-settings
email-validator

# Auth (password hashing + JWT tokens)
bcrypt
python-jose[cryptography]
python-multipart

# Rate limiting (stops brute-force attempts on login)
slowapi

# Face recognition (identity check)
insightface
onnxruntime
# Pinned: opencv-python 5.x dropped the bundled haarcascade data files that DeepFace needs
opencv-python==4.10.0.84
numpy

# Real-time monitoring (websocket connection)
websockets
